/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "lh3.googleusercontent.com",
          "tailwindcss.com",
          "tailwindcss.com.plus-assets.img",
          "images.unsplash.com"
        ],
      },
};

export default nextConfig;
